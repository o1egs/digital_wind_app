class Task {
  int index;
  String title;
  String description;
  String url;
  bool started;
  bool competed;

  Task(this.index, this.title, this.description, this.url, this.started,
      this.competed);
}